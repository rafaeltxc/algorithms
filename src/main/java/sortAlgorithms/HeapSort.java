package sortAlgorithms;

import java.util.List;

public class HeapSort {

	private List<Integer> maxHeapify(List<Integer> arr, int i) {
		
		return arr;
	}
	
	private List<Integer> buildHeap(List<Integer> arr) {
				
		return arr;
	}
	
	public List<Integer> heapSort(List<Integer> arr) {
		if (arr.size() <= 1) { return arr; }
		
		return buildHeap(arr);
	}
	
}
